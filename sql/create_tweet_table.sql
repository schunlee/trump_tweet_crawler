CREATE TABLE TWEET(
    ID             TEXT                   NOT NULL,
    TITLE          TEXT                   NOT NULL,
    TRANS_TITLE          TEXT                   NOT NULL,
    TIME_STR       TEXT                   NOT NULL
    );
